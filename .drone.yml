kind: pipeline
name: default

steps:
- name: build for Linux
  image: rust:latest
  commands:
  - cargo build --release

- name: publish
  image: plugins/github-release
  settings:
    api_key:
      from_secret: github-token
    files: target/release/netio
    checksum:
      - sha256
  when:
    event: tag
